server:
  port: 8761
eureka:
  client:
    register-with-eureka: false       #是否注册（因为当前这个是注册中心，不能把自己注册了，所以false）
    fetch-registry: false       #是否同步其他注册中心的数据
    service-url:
      defaultZone: http://localhost:8761/eureka/        #注册中心的默认访问地址